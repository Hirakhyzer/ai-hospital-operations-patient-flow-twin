name: Python checks

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    env:
      PYTHONPATH: src
      MPLBACKEND: Agg
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install -r requirements.txt
          python -m pip check
      - name: Show environment
        run: |
          python --version
          python -c "import sys; print(sys.path)"
          python -c "import hospitaltwin; print(hospitaltwin.__version__)"
      - name: Run tests
        run: python -m pytest -q tests
      - name: Run synthetic hospital operations smoke test
        run: python scripts/run_synthetic_hospital_lab.py --units 5 --arrivals 90 --hours 24 --seed 7 --output-dir outputs_ci
